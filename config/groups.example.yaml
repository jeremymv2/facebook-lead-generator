# Copy this file to config/groups.yaml, which is intentionally ignored because it can contain
# private group details. Replace the example URL, then explicitly enable only approved groups.
groups:
  - id: louisville-homeowners-example
    name: Louisville Homeowners (example only)
    url: https://www.facebook.com/groups/REPLACE_ME
    enabled: false
    priority: 1
    geography: Louisville, Kentucky
